// Build-time globals injected via Vite's `define` (see vite.config.ts and
// vitest.config.ts). __APP_VERSION__ is the version string from package.json
// at build/test time.
declare const __APP_VERSION__: string;
